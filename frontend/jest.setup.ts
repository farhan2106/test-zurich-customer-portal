// @testing-library/jest-dom extends Jest matchers for DOM testing.
// Imported here as a setup file so matchers like toBeInTheDocument() are available.
// Only needed for component tests; auth slice tests don't require these matchers.
